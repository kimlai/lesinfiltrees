backend:
  name: git-gateway

media_folder: "images/uploads"

collections:
  - name: "pages"
    label: Pages
    files:
    - name: "/"
      label: "Accueil"
      file:  "content/home.yml"
      fields:
        - {label: Texte, name: body, widget: markdown}
